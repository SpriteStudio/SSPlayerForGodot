# Documentation toolchain (MkDocs). Pinned for reproducible CI builds.
# Bump deliberately; keep in sync with the plugins declared in mkdocs.yml.
mkdocs-material==9.7.6
mkdocs-static-i18n==1.3.1
mkdocs-callouts==1.16.1
