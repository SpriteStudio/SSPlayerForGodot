[**日本語**](./USAGE.ja.md) | [**English**](./USAGE.md)

> **Note:** The APIs and workflows described here are still under active development and may change without notice.
> A migration guide for v1.x users will be provided as a separate document.

# Choosing the editor

Pick one of the following approaches.

## Use a Godot Engine with the SpriteStudioPlayer custom module built in

See [BUILD.md](BUILD.md) and build a custom-module Godot Engine.

## Use the SpriteStudioPlayer GDExtension

1. Download the matching version of Godot Engine from the official site (https://godotengine.org/download/).
2. Prepare the SpriteStudioPlayer GDExtension files. To build the GDExtension yourself, see [BUILD.md](BUILD.md).
3. Place the GDExtension files (`.gdextension` and the per-platform shared libraries) under your project's `bin` directory.

# Importing SpriteStudio data

A SpriteStudio project (`.sspj`) is converted to **`.ssab` (animation binary)** and **`.ssqb` (sequence binary)**, placed inside the Godot project, and consumed from `GdSsPlayerNode2D`.
There are two conversion methods. Files generated by either method are loaded as `GdSsabResource` / `GdSsqbResource`.

## Method A: Use the SS Import Dock inside the Godot Editor

When the editor starts, an import control (`GdSsImportControl`) is added to the project dock.
First, configure the output directory for the conversion artifacts.

* Default output directory: `res://ssab_generated`
* Setting key (project settings): `spritestudio/output_directory`
* Recent files key: `spritestudio/recent_sspj_files`

The output directory can be changed via the `Browse` button or the input field on the import control.

Conversion is triggered by either of the following:

* Drag and drop a `.sspj` file onto the editor window
* Re-run from the recent files history on the import control

Progress is shown via `GdProgressDialog`. When complete, `.ssab` (per anime pack) and `.ssqb` (per sequence) files are generated under the output directory.

## Method B: Use the SpriteStudio7-SDK `ssconverter-cli` directly

Download `ssconverter-cli` for your target platform from the [SpriteStudio7-SDK Releases](https://github.com/SpriteStudio/SpriteStudio7-SDK/releases) and convert `.sspj` from the command line.

**Windows:**
```powershell
.\ssconverter-cli.exe path\to\your.sspj
```

**macOS / Linux:**
```bash
./ssconverter-cli path/to/your.sspj
```

The conversion results are written to `<sspj name>_ssab/` next to the source `.sspj`, as `.ssab` / `.ssqb` files.
Copying (or symlinking) the generated files into your Godot project makes them loadable as `GdSsabResource` / `GdSsqbResource`.

This is useful when integrating conversion into a CI / build pipeline, or when running conversions without launching the Godot editor. For detailed `ssconverter-cli` options, see [`SpriteStudio7-SDK/cli/README.md`](https://github.com/SpriteStudio/SpriteStudio7-SDK/blob/main/cli/README.md).

# Creating the SpriteStudio node

1. Add a **`GdSsPlayerNode2D`** node to your scene (Node2D family).
2. Set the inspector's **Ssab Resource** property to an imported `.ssab` file (use `Load`).
3. Set the **Animation** property to the animation name to play. The animation names contained in the `.ssab` are listed in a dropdown.
4. Adjust playback-related properties (Frame, Speed, Loop, Playing, etc.) in the inspector for previewing.

## Main inspector properties

| Property                  | Type     | Description                                                          |
| ------------------------- | -------- | -------------------------------------------------------------------- |
| `Ssab Resource`           | Resource | The `GdSsabResource` (`.ssab`) to play                               |
| `Animation`               | String   | Currently selected animation name                                    |
| `Frame`                   | float    | Current frame position                                               |
| `Speed`                   | float    | Playback speed multiplier (default: 1.0)                             |
| `Frame Rate`              | int      | FPS                                                                  |
| `Loop`                    | int      | Loop count (`0` for infinite loop)                                   |
| `Playing`                 | bool     | Playback flag                                                        |
| `Animation Section Start` | int      | Partial-playback start frame                                         |
| `Animation Section End`   | int      | Partial-playback end frame                                           |
| `Playback Direction`      | int      | Playback direction                                                   |
| `Playback Style`          | int      | Playback style (one-way / round-trip etc.)                           |
| `Skip Frames`             | bool     | Whether to skip frames when the draw interval exceeds the frame interval |
| `Sub Frame Enabled`       | bool     | Whether to enable sub-frame interpolation                            |

For exact behavior and accepted argument values, refer to `gd_spritestudio/gd_ssplayer_node2d.h`.

# Classes

The main classes that can be controlled from GDScript.
For all methods, properties, and signals, see the Godot script documentation.

## Resource classes

These extend the `GdResource` family. When multiple `GdSsPlayerNode2D` nodes share the same resource, set the **Local To Scene** flag to `True` to give each node its own state.

### [GdSsabResource](./gd_spritestudio/gd_ssab_resource.h)

Resource class corresponding to `.ssab` (animation binary).

Main methods:

* `load_from_file(path: String) -> Error`
* `save_to_file(path: String) -> Error`
* `is_valid() -> bool`
* `get_animation_count() -> int`
* `get_animation_names() -> PackedStringArray`

### [GdSsqbResource](./gd_spritestudio/gd_ssqb_resource.h)

Resource class corresponding to `.ssqb` (sequence binary).

Main methods:

* `load_from_file(path: String) -> Error`
* `save_to_file(path: String) -> Error`

## Playback class: [GdSsPlayerNode2D](./gd_spritestudio/gd_ssplayer_node2d.h)

A playback node extending `Node2D`.
Specify a resource and an animation, then start playback.

```gdscript
@onready var ssnode: GdSsPlayerNode2D = $target

func _ready() -> void:
    # Load the .ssab and assign it as the resource
    var ssab: GdSsabResource = ResourceLoader.load("res://ssab_generated/Sample.ssab")
    ssnode.set_ssab_resource(ssab)

    # Specify the animation name
    ssnode.set_animation("anime_1")

    # Play
    ssnode.set_loop(0)        # 0 = infinite loop
    ssnode.set_speed(1.0)
    ssnode.play()
```

Main methods:

* `set_ssab_resource(res: GdSsabResource)` / `get_ssab_resource() -> GdSsabResource`
* `set_animation(name: String)` / `get_animation() -> String`
* `play(start_frame: float = -1.0)` / `pause()` / `stop()`
* `is_playing() -> bool` / `is_pausing() -> bool`
* `set_frame(frame: float)` / `get_frame() -> float` / `get_total_frames() -> int`
* `set_speed(speed: float)` / `get_speed() -> float`
* `set_frame_rate(fps: int)` / `get_frame_rate() -> int`
* `set_animation_section(start: int, end: int)`
* `set_playback_direction(direction: int, style: int)`
* `set_loop(count: int)` / `get_loop() -> int`
* `set_skip_frames(enabled: bool)` / `is_skip_frames() -> bool`
* `set_sub_frame_enabled(enabled: bool)` / `is_sub_frame_enabled() -> bool`

# Limitations

## Not supported

* Mask feature

## Current constraints

* **`.sspj` cannot be loaded directly** — pre-conversion to `.ssab` / `.ssqb` is required.
* **Signal / UserData callbacks are not yet wired up** — there are no corresponding bindings in `gd_ssplayer_node2d.h` yet.
* **The import dock is only available in editor builds (TOOLS_ENABLED)** — template builds do not include the control.
* Draw mode is mix-equivalent only.
* Only some of the official SpriteStudio shaders are supported (custom shaders require additional integration).
